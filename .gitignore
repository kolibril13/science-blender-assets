# Generated by `blender -c asset_listing generate` — rebuilt in CI, not committed.
#
# `assets/_asset-library-meta.json` is deliberately NOT ignored: it is the source
# of the library name and contact details. The generator preserves those fields
# and only refreshes the `api_versions` block, so the committed copy keeps
# `"api_versions": {}`.
assets/_v1/
assets/*_thumbnails/

# Created by `blender -c asset_listing download` when testing the library locally.
_asset_download_location/
